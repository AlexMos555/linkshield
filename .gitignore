# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# ── Environment / Secrets (CRITICAL — never commit) ─
.env
.env.*
!.env.example
*.pem
*.key
*.crt
*.p12
*.pfx
*.keytab
secrets/
credentials.json
service-account*.json
.aws/
.gcp/
.pytest_cache/
.coverage
htmlcov/
coverage.xml
.mypy_cache/

# Data (large files)
data/top-1m.csv
data/phishtank.csv
data/feature_log.jsonl
data/bloom_top100k.json
# data/phishing_model.cbm — TRACKED (238 KB). Authoritative model for retraining
# + scoring tests. Retrain via `ml/train_model.py`; commit the .cbm.
# data/phishing_model.onnx — TRACKED (520 KB). What PROD actually runs
# (onnxruntime, ~40 MB RSS vs catboost ~400 MB). Regenerated alongside the .cbm
# by ml/train_model.py; commit both together so they never drift.

# CatBoost training artifacts
catboost_info/

# Node
node_modules/
landing/.next/
landing/out/

# Extension build
linkshield-extension.zip

# OS
.DS_Store
Thumbs.db
# macOS Finder / iCloud creates "filename 2.ext" duplicates when there's
# a name collision (e.g. unzip into a dir that already has the file).
# Catch them all so they never sneak into commits.
* 2.zip
* 2.png
* 2.json
* 2.tsx
* 2.ts
* 2.jsx
* 2.js
* 2.py
* 2.md

# IDE
.vscode/
.idea/
*.swp
*.swo

# Claude Code / AI assistants
.claude/
.cursor/
.ecc/

# --- Android/iOS release signing — NEVER commit (permanent secret material) ---
# The release keystore identifies the app forever; committing it (or its
# passwords) is unrecoverable. Root .gitignore had *.key/*.p12/*.keytab but not
# these, and the managed native dirs (regenerated by `expo prebuild`) were not
# ignored either — so a `git add -A` after following docs/RUSTORE_SUBMISSION.md
# would have staged the key. (Found by the 2026-08-25 funnel-reality audit.)
*.jks
*.keystore
keystore.properties
# Managed workflow: mobile/android and mobile/ios are generated by prebuild and
# must not be tracked (this is also what keeps a keystore dropped inside android/ out of git).
/mobile/android/
/mobile/ios/
