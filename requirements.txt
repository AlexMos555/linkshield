fastapi==0.115.0
uvicorn[standard]==0.32.0
httpx==0.28.0
redis[hiredis]==5.2.0
pydantic-settings==2.6.0
PyJWT==2.10.0
python-multipart==0.0.18
dnspython==2.7.0
stripe==11.0.0
sentry-sdk[fastapi]==2.19.0
bcrypt==4.2.1
# ML scoring — wraps the CatBoost model in data/phishing_model.cbm.
# Pulls ~120 MB of numpy/scipy wheels; keep pinned so Railway build stays
# reproducible. If catboost upstream drops Python 3.11 support we fall
# back gracefully (ml_scorer._load_model catches ImportError).
catboost==1.2.7
numpy==1.26.4
