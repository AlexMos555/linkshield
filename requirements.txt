fastapi==0.115.0
uvicorn[standard]==0.32.0
httpx==0.28.1
redis[hiredis]==5.2.0
pydantic-settings==2.6.0
PyJWT==2.12.1
python-multipart==0.0.18
dnspython==2.8.0
stripe==11.0.0
sentry-sdk[fastapi]==2.19.0
bcrypt==4.2.1
# NOTE: ML scoring (catboost + numpy) is NOT installed here.
# Reason: catboost resident footprint is ~400MB — OOM-kills pods on Railway's
# 512 MB starter plan. Without it, ml_scorer._load_model() gracefully fails
# and scoring drops to rule-based only (obvious phishing still reaches
# "suspicious" level but not "dangerous").
#
# To re-enable ML once we're on a larger plan (Pro / 1 GB+):
#   pip install catboost==1.2.7 numpy==1.26.4
#
# Or wire ML as a separate worker service (Phase I) so API stays lean.
